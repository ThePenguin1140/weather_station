# InfluxDB 2 Persistence Configuration
# Deployed by deploy_openhab.py to /etc/openhab/services/influxdb.cfg
#
# Token is substituted from config.json by the deploy script.
# Do NOT put a real token here — use config.json instead.

version=2
url=http://localhost:8086
token=PLACEHOLDER_REPLACE_WITH_TOKEN
organization=openhab
bucket=weatherstation
